"""Tools do MCP SEI, organizadas por domínio.

Cada submódulo importa o `mcp` compartilhado e os helpers de `todos.mcp_app` e
registra suas tools via `@mcp.tool`. O `todos.server` importa todos os
submódulos para disparar o registro.
"""
